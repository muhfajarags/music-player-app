<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/> <!-- Warna background putih saat dipilih -->
            <size android:width="68dp" android:height="68dp"/> <!-- Ukuran background lingkaran lebih kecil -->
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/> <!-- Background transparan saat tidak dipilih -->
            <size android:width="68dp" android:height="68dp"/> <!-- Ukuran yang sama untuk transparan -->
        </shape>
    </item>
</selector>
